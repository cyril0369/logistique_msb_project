# React Migration Starter

This folder starts the HTML-to-React migration for `secure-login`.

- Generated components: 23
- Source of truth manifest: `react-migration/src/legacyHtmlPages.js`

Each component in `react-migration/src/pages` corresponds to one existing `.html` file and is ready to be replaced with real JSX.
